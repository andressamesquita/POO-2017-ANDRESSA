<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andressaribeiro.myfirstapp">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CadastroAlunoActivity" />
        <activity android:name=".TelaInicialActivity"></activity>
        <activity android:name=".ListaDisciplinasActivity" />
        <activity android:name=".CadastroDisciplinaActivity" />
        <activity android:name=".ListaBimestreActivity"></activity>
    </application>

</manifest>